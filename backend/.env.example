# Supabase pooled connection string (recommended on networks where direct host DNS fails)
# Replace placeholders with your actual credentials from Supabase > Connect > Pooler.
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@<pooler-host>:6543/postgres?sslmode=require
